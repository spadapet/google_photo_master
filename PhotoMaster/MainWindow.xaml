<Window
    x:Class="PhotoMaster.MainWindow"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:PhotoMaster.Models"
    mc:Ignorable="d"
    Title="Photo Master"
    Height="450"
    Width="800"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    DataContext="{Binding ViewModel, RelativeSource={RelativeSource Mode=Self}}"
    d:DataContext="{d:DesignInstance models:MainViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="4">
        <Button Content="Test OAuth" Command="{Binding TestAuthCommand}" Padding="4" HorizontalAlignment="Left" VerticalAlignment="Top" />
    </Grid>
</Window>
